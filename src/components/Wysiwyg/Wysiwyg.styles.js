import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    textOutput:{
        position:"absolute",
        backgroundColor:"rgba(255, 5, 10, 0.5)",
        width:500,
        height:29,
        top: 0

    }
}));
